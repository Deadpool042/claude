server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        try_files /502.html =404;
    }

    location /not-found {
        root /usr/share/nginx/html;
        try_files /404.html =404;
    }
}
