# Tests manuels — structure

- `combos/` : couvre les combinaisons fonctionnelles (type projet × modules × stacks).

Ajoute les nouveaux tests dans le dossier pertinent pour garder une lecture claire.
